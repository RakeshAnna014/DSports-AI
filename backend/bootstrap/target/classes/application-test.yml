spring:
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:dsports_test}
    username: ${DB_USER:dsports}
    password: ${DB_PASSWORD:dsports123}

  flyway:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:dsports_test}
    user: ${DB_USER:dsports}
    password: ${DB_PASSWORD:dsports123}
    enabled: true

logging:
  level:
    com.dsports: DEBUG

management:
  endpoint:
    health:
      show-details: always
      show-components: always
